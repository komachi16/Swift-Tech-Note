# Swift-Tech-Note

### Setup

#### 必要なツールのインストール

このプロジェクトでは、`swiftformat` を使用しています。以下のコマンドで Homebrew を使ってインストールしてください。

```bash
brew install swiftformat
```

#### スクリプトの実行

次に、セットアップスクリプトを実行します。

```
% chmod +x setup.sh
% ./setup.sh
```
